<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Smooth Animation Durations -->
    <Duration x:Key="FastDuration">0:0:0.15</Duration>
    <Duration x:Key="MediumDuration">0:0:0.25</Duration>
    <Duration x:Key="SlowDuration">0:0:0.35</Duration>

    <!-- Easing Functions -->
    <CubicEase x:Key="SmoothEaseOut" EasingMode="EaseOut"/>
    <CubicEase x:Key="SmoothEaseInOut" EasingMode="EaseInOut"/>
    <QuadraticEase x:Key="QuickEaseOut" EasingMode="EaseOut"/>

    <!-- Fade In Animation -->
    <Storyboard x:Key="FadeInAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="{StaticResource MediumDuration}"
                        EasingFunction="{StaticResource SmoothEaseOut}"/>
    </Storyboard>

    <!-- Fade Out Animation -->
    <Storyboard x:Key="FadeOutAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="1" To="0"
                        Duration="{StaticResource FastDuration}"
                        EasingFunction="{StaticResource SmoothEaseOut}"/>
    </Storyboard>

    <!-- Slide In From Bottom -->
    <Storyboard x:Key="SlideInFromBottom">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                        From="20" To="0"
                        Duration="{StaticResource MediumDuration}"
                        EasingFunction="{StaticResource SmoothEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="{StaticResource MediumDuration}"
                        EasingFunction="{StaticResource SmoothEaseOut}"/>
    </Storyboard>

    <!-- Scale Up Animation -->
    <Storyboard x:Key="ScaleUpAnimation">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        From="0.95" To="1"
                        Duration="{StaticResource MediumDuration}"
                        EasingFunction="{StaticResource SmoothEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        From="0.95" To="1"
                        Duration="{StaticResource MediumDuration}"
                        EasingFunction="{StaticResource SmoothEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="{StaticResource MediumDuration}"/>
    </Storyboard>

    <!-- Hover Lift Animation Storyboard -->
    <Storyboard x:Key="HoverLiftAnimation">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                        To="-4"
                        Duration="{StaticResource FastDuration}"
                        EasingFunction="{StaticResource QuickEaseOut}"/>
    </Storyboard>

    <!-- Hover Return Animation Storyboard -->
    <Storyboard x:Key="HoverReturnAnimation">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                        To="0"
                        Duration="{StaticResource FastDuration}"
                        EasingFunction="{StaticResource QuickEaseOut}"/>
    </Storyboard>

    <!-- Pulse Animation -->
    <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="1" To="0.6" AutoReverse="True"
                        Duration="0:0:1"
                        EasingFunction="{StaticResource SmoothEaseInOut}"/>
    </Storyboard>

    <!-- Shimmer Loading Animation -->
    <Storyboard x:Key="ShimmerAnimation" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                        From="-200" To="200"
                        Duration="0:0:1.5"
                        EasingFunction="{StaticResource SmoothEaseInOut}"/>
    </Storyboard>

    <!-- Gradient Backgrounds -->
    <LinearGradientBrush x:Key="OrangeGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#F59E0B" Offset="0"/>
        <GradientStop Color="#D97706" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="OrangeGradientLight" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FDE68A" Offset="0"/>
        <GradientStop Color="#FCD34D" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="SuccessGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#10B981" Offset="0"/>
        <GradientStop Color="#059669" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="InfoGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#3B82F6" Offset="0"/>
        <GradientStop Color="#2563EB" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="WarningGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#F59E0B" Offset="0"/>
        <GradientStop Color="#D97706" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="DangerGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#EF4444" Offset="0"/>
        <GradientStop Color="#DC2626" Offset="1"/>
    </LinearGradientBrush>

    <!-- Glassmorphism Effect -->
    <SolidColorBrush x:Key="GlassBrush" Color="#F0FFFFFF" Opacity="0.7"/>

    <!-- Enhanced Card Shadow with Blur -->
    <DropShadowEffect x:Key="ElevatedCardShadow"
                      BlurRadius="20"
                      ShadowDepth="4"
                      Opacity="0.08"
                      Color="#000000"
                      RenderingBias="Quality"/>

    <DropShadowEffect x:Key="FloatingCardShadow"
                      BlurRadius="24"
                      ShadowDepth="8"
                      Opacity="0.12"
                      Color="#000000"
                      RenderingBias="Quality"/>

    <!-- Glow Effects -->
    <DropShadowEffect x:Key="OrangeGlow"
                      BlurRadius="16"
                      ShadowDepth="0"
                      Opacity="0.4"
                      Color="#D97706"/>

    <DropShadowEffect x:Key="SuccessGlow"
                      BlurRadius="16"
                      ShadowDepth="0"
                      Opacity="0.4"
                      Color="#10B981"/>

</ResourceDictionary>
